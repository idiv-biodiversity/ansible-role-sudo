---

argument_specs:
  main:
    options:

      sudo_disable_wheel:
        required: yes
        type: bool

      sudo_sudoers:
        type: list
        elements: dict
        options:
          name:
            required: yes
            type: str
          host:
            type: str
          runas:
            type: str
          command:
            type: str
          secure_path:
            type: list
            elements: str
          timestamp_timeout:
            type: int

...
