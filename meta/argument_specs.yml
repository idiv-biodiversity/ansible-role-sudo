---

argument_specs:
  main:
    options:

      sudo_disable_wheel:
        required: yes
        type: bool

      sudo_sudoers:
        type: list
        elements: dict
        options:
          name:
            type: str
            required: yes

...
